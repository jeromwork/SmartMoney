# Terminal Layout

- `mt4-dev` — ручная отладка, загрузка истории, проверка графиков
- `mt4-test` — CLI-бэктесты
- `mt4-demo` — demo-forward test

Эти каталоги намеренно разделены, чтобы Codex мог безопасно запускать тесты и demo без смешивания состояния.
